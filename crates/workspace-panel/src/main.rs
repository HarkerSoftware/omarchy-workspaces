//! `workspace-panel`: GTK4 layer-shell left rail for omarchy-workspaces.
//!
//! Implemented in milestone M-P1; until then this is a stub so the workspace
//! builds end-to-end.

fn main() -> anyhow::Result<()> {
    println!(
        "workspace-panel {} (not yet functional)",
        env!("CARGO_PKG_VERSION")
    );
    Ok(())
}
